---
dependencies:
  - { role: eye, when: "not ((ansible_distribution == 'Debian' and (ansible_distribution_version|version_compare(8.0, '>'))) or (ansible_distribution == 'Ubuntu' and (ansible_distribution_version|version_compare(14.04, '>'))))" }
